{
  "name": "io-filecoin",
  "private": true,
  "description": "Filecoin protocol client in portable .cljc — addresses, messages, signatures, Lotus JSON-RPC",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljs",
    "vectors": "nbb testdata/gen_vectors.cljs > test/filecoin/vectors.cljc"
  },
  "dependencies": {}
}
